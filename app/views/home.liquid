<h1>{{ title }}</h1>

<h2>API endpoints</h2>

<ul>
{% for name in endpoints %}
  <li><a href="/v1/{{ name }}">{{ name }}</a></li>
{% endfor %}
</ul>

{% if examples.size > 0 %}
  <h2>Examples</h2>

  <ul>
  {% for ex in examples %}
    <li><a href="{{ ex.link }}">{{ ex.name }}</a> {{ ex.description }}</li>
  {% endfor %}
  </ul>
{% endif %}

<h2>Browse Data Details by Category</h2>
<div class="categories">
  <div id="categories-list" class="categories__column"></div>
</div>

<script>
  var categories = {{ categories }} ;
  var oneColumn = "";
  for (var i in categories) {
    oneColumn = oneColumn + "<div class=\"category__name\">" + categories[i].title + "</div>" + 
      "<p>" + categories[i].description + "</p>";
  }
//  alert(oneColumn);
  document.getElementById("categories-list").innerHTML=oneColumn;
</script>

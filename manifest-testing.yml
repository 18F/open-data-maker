---
applications:
-  name: ccapi
   host: ccapi-testing
   domain: 18f.gov
   command: bundle exec puma -C ./config/puma.rb
   instances: 1
   memory: 1G
   services:
   - data-files
   - eservice
   env:
     MAX_THREADS: 5
     WEB_CONCURRENCY: 1
     INDEX_APP: enable
     NPROCS: 1
